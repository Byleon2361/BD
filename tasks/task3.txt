Задание на 24.09:
1. Доработать замечания
2. Создать crud операции для каждой таблицы.
3. Разработать бизнес кейсы, та что бы реализующие их запросы отвечали следующим требованиям:
- 3-5 агригрегирующих запроса
- 3-5 оконных функции
- 8 запросов из которых:
    - 2 запроса через объединение 2 таблиц
    - 4 запроса через объединение 3 таблиц
    - 1 запрос объединение 4 таблиц
    - 1 запрос объединение 5 таблиц. 
4. Сгенерировать данные для ваших таблиц. Должно быть 3-3.5 млн записей в самой большой таблице. Записи не должны быть мёртвыми.

-------------
## Задание 3
Я создал скрипт, который генерирт 2 sql файла
complete_news_database_postgres.sql - основаня база данных, веит 68 Мбайт, поэтому может быть там не 3 млн записей
business_queries_postgres.sql - это тествой запросы.

Команда выводит все бд в контейнере
docker exec -it bd-postgres-1 psql -U postgres -l

Эта команда выполняет все тестове запросы, которые описаны в задании 3.
 docker exec -i bd-postgres-1 psql -U postgres -d news_aggregator -f - < aggregating_queries.sql
 docker exec -i bd-postgres-1 psql -U postgres -d news_aggregator -f - < window_functions.sql
 docker exec -i bd-postgres-1 psql -U postgres -d news_aggregator -f - < queries_with_joined_tables.sql