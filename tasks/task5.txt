Развернуть Mongo создать БД и коллекции домена

Спроектировать документы (1-2 основные коллекции + 1 вспомогательная)

Загрузить seed-данные (>500 документов суммарно)- `insertMany/mongoimport`

Реализовать базовые операции:
- `insertOne/Many`, `updateOne/Many` (`$set`, `$inc`, `$push/$addToSet`, `$arrayFilters`), `$delete`, `replaceOne`, `upsert`
- Поиск: фильтры с `$and/$or/$in/$nin/&gt/&lt`, проекции полей
- Создать индексы под ключевые запросы: одиночные, составные по массивам, partitial, TTL, unique

Замерить планы выполнения: `explain("executionStats")` до/после индексов (2-3 запроса)

Сделать Rest-слой 3-5 endpoint'ов для демонстрации

Собрать 3-5 `aggregation pipelines` под реальные вопросы домена. Обязательные стадии `$match`, `$project`, `$unwind`, `$group`, `$sort`, `$limit`. Как минимум один `pipeline` с `$lookup` связь 1 -> N, N -> N

Сделать витрину/отчет, зафиксировать результат

Добавить спец-возможности по домену:
- Агрегатор новостей: текстовый индекс и search-запрос, дедубликация по `hash`
- Библиотека: `availability` по экземплярам, топ-авторы (`$unwind`)
- Мероприятия: заполняемость, "sold-out", отчет по площадкам
- Магазин: средний ретинг, фасеты каталога (категории/бренд/цена)
- Спорт центр: заполняемость классов по дням/тренерам, ретеншен
- Транспорт: `$geoNear` для ближайших остановок, опоздания (planned vs actual)

Провести мини-профайлинг: сравнить 1-2 pipeline с/без индекса, описать выводы (2-3 тезиса)

Материализовать 1 агрегат в отдельную коллекцию (витрина)